import { PageShell } from "@/components/ui/page-shell";

export default function SettingsPage() {
  return (
    <PageShell
      title="Settings"
      description="Placeholder workspace for app preferences, access controls, and account configuration."
    />
  );
}
